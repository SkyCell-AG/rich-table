import {
    makeStyles,
} from '@mui/styles'

const useStyles = makeStyles(() => {
    return {
        hiddenCheckAll: {
            visibility: 'hidden',
        },
    }
})

export default useStyles
